{
  "schema": 1,
  "type": "chat",
  "description": "Creates a quiz question based on a subject",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.2
    }
  },
  "input_variables": [
    {
      "name": "subject",
      "description": "The user's subject.",
      "required": true
    }
  ]
}